# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/delightecofarms
# For MongoDB Atlas (cloud), use format: mongodb+srv://username:password@cluster.mongodb.net/database

# Email Configuration for Ubuntu Server (matching your PHP setup)
EMAIL_METHOD=postfix
EMAIL_HOST=localhost
EMAIL_PORT=25
EMAIL_SECURE=false
EMAIL_FROM=sales@delightecofarms.com
EMAIL_TO=sales@delightecofarms.com

# No authentication needed for localhost SMTP (same as your PHP setup)
# EMAIL_USER=
# EMAIL_PASS=

# Application Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
